/**
 * Constants used throughout this project
 */
public interface CONNECT_CONSTANTS {

    // the height of the planes with zero based indexing
    public static final int HEIGHT = 3;

    // the length and width of each 2-D plane
    public static final int PLANE_SIZE = 5;

    // the mid section of a 2-d plane
    public static final int MID_INDEX = PLANE_SIZE / 2;

    // Num in a row needed for a winner (in this case Connect four!)
    public static final int THRESHOLD = 4;




}
